function [Gi,Ge] = Nonlinear_Constraints(a)
% Define the list of global variables
myGlobalVariables

if a == atemp
    Gi = Gineq;
    Ge = Geq;
else
    Gi = Gineq;
    Ge = Geq;
    error = a - atemp;
    disp('== Different a0 == %g',error);
    [C,Gi,Ge] = Cost_Torque(a);
end

end